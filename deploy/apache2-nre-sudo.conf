# NexEditorStats - Bridge page runs sudo from PHP (journalctl / systemctl).
# Ubuntu apache2 defaults RestrictSUIDSGID=yes, which blocks setuid sudo
# in the Apache cgroup. CLI sudo -u www-data still works without this.
[Service]
RestrictSUIDSGID=no
